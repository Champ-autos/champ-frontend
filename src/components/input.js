import React from "react"

const Input = () => (
    <input style={inputStyle} placeholder="Make, model, or body"/>
    )


const inputStyle = {
    borderRadius: 36,
    background:"white",
    padding: "16px 24px",
}

export default Input